{
  "name": "FloraliaMERN",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start --prefix server",
    "build": "npm install --legacy-peer-deps --prefix client && npm install --prefix server && npm run build --prefix client && npm run build --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
